const fs = require("fs");
const path = require("path");

const filePath = path.join(__dirname, "..", "courses", "drone_training.json");
const data = JSON.parse(fs.readFileSync(filePath, "utf-8"));

const learningScript = [
  {
    page: 1,
    title: "Introduction: The Real-Time Imperative for Drone Perception",
    content: "Autonomous drones operate under physics constraints that software engineers rarely encounter. A drone flying at 15 meters per second covers 15 centimeters in a single millisecond. Every millisecond of processing delay translates directly into distance traveled without updated perception data. When the total perception loop exceeds the safe reaction budget, the drone becomes blind to obstacles that enter its path during the processing interval.\n\nStandard deep learning frameworks treat inference as a batch processing problem optimized for throughput. They accept variable-latency overheads from Python interpretation, dynamic memory management, and implicit GPU synchronization because these overheads are amortized across large batches. Drone perception inverts this priority: single-frame latency with bounded worst-case execution time matters more than throughput optimized for average case.\n\nBare-metal CUDA programming addresses this inversion by giving the engineer direct control over every layer of the execution stack. The term bare-metal in this context means writing GPU code that executes without framework intermediation. No Python interpreter dispatches the kernel. No autograd system tracks gradients. No memory pool manager decides when to allocate or free GPU memory. The engineer specifies exactly which GPU resources to use, when to transfer data, how to partition work across thread blocks, and where to store intermediate results.\n\nThe penalty for this control is development complexity. A single CUDA kernel implementing image preprocessing requires understanding thread block geometry, memory coalescing patterns, shared memory bank structure, warp execution semantics, and synchronization primitives. A bug in any of these produces silent data corruption or non-deterministic crashes. The code is hardware-specific and requires retuning for each GPU architecture.\n\nThis lesson takes a different angle from the previous coverage of custom CUDA NMS kernels. Here the focus is on image preprocessing: the undistortion, resizing, color conversion, and normalization operations that prepare raw camera frames for neural network inference. In a typical drone pipeline, these operations are performed by OpenCV running on the CPU, consuming 3-8 milliseconds per frame. Moving them to a single fused CUDA kernel reduces this to 200-500 microseconds while also eliminating the CPU-to-GPU data transfer that would otherwise follow the CPU preprocessing.\n\nThe goal is not to replace every framework function with hand-written CUDA. The goal is to identify the specific pipeline stages where framework overhead violates timing constraints and replace those stages with deterministic bare-metal implementations. This lesson teaches the methodology for making that determination and executing the replacement.",
    key_takeaways: [
      "Every millisecond of processing delay translates to physical distance traveled without updated perception",
      "Bare-metal CUDA gives direct control over execution but requires understanding thread geometry, memory, and synchronization",
      "Fused CUDA preprocessing reduces 3-8ms CPU OpenCV operations to 200-500 microseconds while eliminating host-device transfer"
    ]
  },
  {
    page: 2,
    title: "Core Concept: The CUDA Execution Model for Image Processing",
    content: "Image preprocessing maps naturally to GPU parallelism because each output pixel can be computed independently. For undistortion, each output pixel's value is determined by sampling the input image at a location computed from the camera's distortion model. For resizing, each output pixel is computed by interpolating nearby input pixels. For normalization, each output pixel is scaled and shifted by constants. These are embarrassingly parallel operations where the GPU's thousands of threads each handle one or more output pixels.\n\nThe CUDA grid and block dimensions are configured to match the output image dimensions. For a 640x640 output image with 16x16 thread blocks, the grid has 40x40 blocks, launching 409,600 threads total. Each thread computes one output pixel across all three color channels. The thread's output pixel coordinates are derived from blockIdx and threadIdx: out_x = blockIdx.x * blockDim.x + threadIdx.x and out_y = blockIdx.y * blockDim.y + threadIdx.y.\n\nThe memory access pattern determines performance. For undistortion, each thread reads from a potentially different location in the input image, determined by the inverse distortion mapping. These reads are spatially scattered, resulting in uncoalesced global memory access. The GPU's L2 cache mitigates this partially, but performance suffers compared to the ideal case of sequential memory access.\n\nTexture memory provides hardware-accelerated interpolation and spatial caching for 2D access patterns. By binding the input image to a CUDA texture object, the hardware handles bilinear interpolation automatically and caches spatial neighborhoods in a 2D-optimized cache structure. For undistortion where nearby output pixels sample nearby input regions, texture caching significantly improves memory throughput compared to raw global memory reads.\n\nThe alternative to texture memory is shared memory with explicit cooperative loading. A thread block loads a tile of the input image into shared memory, then all threads in the block sample from the shared memory tile. This requires computing the bounding box of input pixels needed by the block's output pixels (accounting for distortion displacement), loading that region cooperatively, synchronizing with __syncthreads(), and then computing output pixels from shared memory. This approach provides maximum control but requires careful boundary handling when the input region extends beyond the loaded tile.\n\nMemory bandwidth on the Jetson Orin Nano (approximately 68 GB/s shared between CPU and GPU) is the primary performance constraint for image processing kernels. A 1920x1080 RGB input image is 6.2 MB. Reading it once and writing the 640x640 output (1.2 MB) at the theoretical bandwidth peak would take 0.1 milliseconds. In practice, cache misses, uncoalesced access, and memory controller overhead increase this to 0.3-0.5 milliseconds for a well-optimized kernel. If the measured kernel time significantly exceeds this range, the kernel has optimization opportunities in its memory access pattern.\n\nBlock size selection for 2D image kernels considers both the thread count and the aspect ratio. A 16x16 block (256 threads) provides good 2D spatial locality because threads in the same block process a 16x16 pixel region, maximizing cache reuse for overlapping input regions. A 32x8 block (also 256 threads) provides better memory coalescing for row-major image layouts because 32 consecutive threads access 32 consecutive pixels in the same row, perfectly coalescing into a single 128-byte memory transaction.",
    key_takeaways: [
      "Image preprocessing is embarrassingly parallel: each output pixel can be computed independently by one GPU thread",
      "Texture memory provides hardware-accelerated interpolation and 2D spatial caching ideal for scattered undistortion reads",
      "Memory bandwidth (68 GB/s on Jetson) is the primary constraint; coalesced access patterns maximize utilization",
      "16x16 or 32x8 thread blocks balance 2D spatial locality with row-major memory coalescing"
    ]
  },
  {
    page: 3,
    title: "Core Concept: Fused Image Preprocessing Kernels",
    content: "A fused preprocessing kernel performs undistortion, resizing, color conversion, and normalization in a single GPU pass. Without fusion, each operation is a separate kernel: the undistortion kernel reads the input and writes an intermediate image, the resize kernel reads that intermediate and writes another, the color conversion kernel reads and writes again, and the normalization kernel performs a final pass. Each intermediate image consumes GPU memory bandwidth twice (once for writing, once for reading), and each kernel launch adds overhead.\n\nThe fused kernel computes the final output pixel value by chaining all four transformations in a single thread. Starting from the output pixel coordinates, the thread computes the pre-resize coordinates (scaling by the original/target ratio), then computes the pre-distortion coordinates (applying the inverse distortion model), samples the input image at the computed location (bilinear interpolation via texture fetch), converts the pixel from BGR to RGB (channel reorder), and normalizes the value (subtract mean, divide by standard deviation). The entire chain executes in registers without any intermediate global memory writes.\n\nThe computational cost per pixel for the fused kernel is dominated by the distortion model evaluation. A standard Brown-Conrady radial distortion model involves: computing radial distance from the optical center, evaluating a polynomial (typically degree 4-6 with coefficients k1 through k6), applying tangential distortion corrections (p1, p2), and computing the undistorted source coordinates. This requires approximately 20-30 floating-point operations per pixel, well within the compute budget given the memory bandwidth constraint.\n\nThe camera distortion coefficients are passed to the kernel as constant memory. CUDA constant memory is cached in a dedicated on-chip cache that broadcasts the same value to all threads reading the same address simultaneously. Since all threads use the same distortion coefficients, this broadcast behavior provides maximum efficiency. The coefficients are set once during camera calibration and do not change during operation.\n\nThe normalization constants (mean and standard deviation per channel) are also stored in constant memory. For ImageNet-normalized models, these are (0.485, 0.456, 0.406) for the mean and (0.229, 0.224, 0.225) for the standard deviation. For models trained on drone-specific imagery, custom normalization constants may be used. These constants must match exactly between the training preprocessing and the deployment kernel to avoid accuracy degradation.\n\nOutput format must match the TensorRT engine's expected input layout. Most detection models expect NCHW format (batch, channels, height, width) with float32 data type. The kernel writes the output in NCHW layout: the red channel for all pixels is contiguous in memory, followed by the green channel, then the blue channel. This planar output layout differs from the input's interleaved BGR format and is handled naturally by the kernel's write indexing: out[0 * H * W + y * W + x] = red, out[1 * H * W + y * W + x] = green, out[2 * H * W + y * W + x] = blue.\n\nThe performance gain from fusion is substantial. Four separate kernels processing a 1920x1080 input to 640x640 output might take 2-3 milliseconds total (including intermediate buffer allocation and kernel launch overhead). The fused kernel achieves the same result in 300-500 microseconds because it reads the input once, writes the output once, and launches one kernel. The memory bandwidth savings alone account for a 4-6x reduction: four passes through the data versus one.\n\nFusion does increase per-thread register usage because each thread maintains all intermediate values. If register pressure becomes too high (exceeding the SM's per-thread register budget), the CUDA compiler spills registers to local memory, degrading performance. Monitoring register usage with the --ptxas-options=-v compiler flag ensures that the fused kernel stays within the hardware's register budget.",
    key_takeaways: [
      "Fused kernels chain undistortion, resize, color conversion, and normalization in a single GPU pass per pixel",
      "The fused approach eliminates intermediate buffer memory traffic, achieving 4-6x bandwidth savings over separate kernels",
      "Camera distortion coefficients and normalization constants are stored in CUDA constant memory for broadcast access",
      "Output must be written in NCHW float32 layout to match TensorRT engine input expectations"
    ]
  },
  {
    page: 4,
    title: "Core Concept: Minimizing Warp Divergence and Memory Bank Conflicts",
    content: "Warp divergence occurs when threads within the same 32-thread warp take different execution paths at a branch. The GPU must execute both paths sequentially, with inactive threads masked. For image preprocessing, divergence arises primarily at image boundaries: threads where the computed source coordinate falls outside the input image take one path (returning a zero or border pixel), while threads with valid source coordinates take another path (performing interpolation).\n\nBoundary divergence is inherent to the problem but can be minimized through kernel design. Instead of checking boundaries per pixel inside the kernel, the grid can be configured to only launch threads for the valid output region. Border handling is performed by a separate, smaller kernel or by padding the input image with border pixels before the main kernel launch. This eliminates the branch entirely from the hot inner kernel.\n\nFor undistortion specifically, warp divergence also occurs when the distortion model maps some pixels to valid input regions and others to out-of-bounds regions. Wide-angle drone cameras with strong barrel distortion have large invalid regions near the image corners. Threads processing corner pixels diverge from threads processing center pixels. Reordering the thread-to-pixel mapping so that warps process contiguous regions (all-center or all-corner) reduces the number of warps that experience divergence, though it does not eliminate it entirely.\n\nShared memory bank conflicts limit the effective bandwidth of shared memory access. The Jetson's GPU organizes shared memory into 32 banks, each 4 bytes wide. If two threads in the same warp access different addresses that map to the same bank, the accesses are serialized (a 2-way conflict halves bandwidth; a 32-way conflict reduces bandwidth to 1/32). For image data stored as interleaved RGB pixels (RGBRGBRGB...), consecutive threads accessing consecutive pixels create a 3-way stride pattern that causes bank conflicts.\n\nThe solution is to store the image tile in shared memory using a padded layout. Adding one padding element per row of the shared memory tile shifts alternate row accesses to different banks, breaking the conflict pattern. For 4-byte (float) elements with 32 banks, a 33-element row width instead of 32 eliminates the most common conflict patterns. The cost is a small increase in shared memory usage per block.\n\nAlternatively, converting from interleaved RGB to planar layout in shared memory avoids bank conflicts entirely. Each thread loads its pixel's RGB values from global memory and writes R, G, and B to three separate shared memory arrays. Subsequent accesses to each array have stride-1 patterns with no bank conflicts. This approach uses slightly more shared memory but provides maximum bandwidth.\n\nCoalesced global memory reads require that threads in a warp access contiguous addresses aligned to 128-byte boundaries. For a row-major image where pixels are stored consecutively, threads accessing consecutive x-coordinates in the same row produce perfectly coalesced reads. However, undistortion's scattered source coordinate computation disrupts this pattern. Texture memory's 2D caching structure mitigates this by caching rectangular neighborhoods, making nearby scattered accesses cache-friendly even when they are not coalesced.\n\nThe practical optimization workflow is: implement the naive kernel, profile with Nsight Compute to measure achieved bandwidth, warp divergence percentage, and bank conflict rate, then apply the specific optimization that addresses the measured bottleneck. Apply one optimization at a time and re-profile to verify improvement. This empirical approach prevents wasting effort on optimizations that do not address the actual performance limiter.",
    key_takeaways: [
      "Boundary-checking branches cause warp divergence; configure the grid to only launch threads for valid output regions",
      "Shared memory bank conflicts from interleaved pixel data are resolved by padding or planar conversion",
      "Texture memory's 2D caching makes scattered undistortion reads cache-friendly despite non-coalesced access patterns",
      "Apply one optimization at a time and re-profile with Nsight Compute to verify measurable improvement"
    ]
  },
  {
    page: 5,
    title: "Practical Implementation: Building and Benchmarking a Fused Preprocessing Kernel",
    content: "Building the fused preprocessing kernel starts with the host-side setup. At initialization, the camera calibration parameters (intrinsic matrix, distortion coefficients) are loaded and copied to GPU constant memory using cudaMemcpyToSymbol. The output buffer is pre-allocated as a contiguous float array sized for NCHW format: 3 * 640 * 640 * sizeof(float) = 4,915,200 bytes. The input image is bound to a CUDA texture object configured for bilinear interpolation and border addressing mode (out-of-bounds reads return zero).\n\nThe kernel launch configuration uses 2D blocks and grid. For a 640x640 output with 16x16 blocks: dim3 block(16, 16) and dim3 grid(40, 40). Each thread computes one output pixel. The kernel receives the texture object, output buffer pointer, output dimensions, input dimensions, and a pointer to the constant memory distortion parameters.\n\nInside the kernel, each thread computes its output coordinates, applies the inverse resize scaling to find the input coordinate at the original resolution, applies the inverse distortion model using the stored coefficients, uses tex2D to fetch the interpolated input pixel at the undistorted coordinate, converts from BGR to RGB by swapping channel indices, subtracts the per-channel mean and divides by the per-channel standard deviation, and writes the normalized value to the output buffer at the NCHW offset.\n\nBenchmarking requires accurate timing methodology. CUDA events record GPU-side timestamps with sub-microsecond resolution. Create two events, record the start event before the kernel launch and the stop event after. Call cudaEventSynchronize on the stop event, then cudaEventElapsedTime to compute the interval. This measures only the GPU kernel execution time, excluding host-side overhead.\n\nA warm-up phase of 50-100 kernel invocations is necessary before timing begins. Early invocations exhibit higher latency due to GPU clock ramp-up, JIT compilation of PTX code, and cold caches. After warm-up, run 1000 timed iterations and compute the mean, standard deviation, minimum, and maximum execution times. The standard deviation indicates jitter: a deterministic kernel should show sub-microsecond standard deviation.\n\nCompare the fused kernel against the baseline: OpenCV CPU preprocessing. The baseline pipeline reads the frame from camera, calls cv::undistort, cv::resize, cv::cvtColor (BGR to RGB), normalizes by subtracting mean and dividing by std, transposes to NCHW, and copies to GPU with cudaMemcpy. Time each step individually and measure the total. The typical breakdown shows undistortion at 2-4 milliseconds, resize at 0.5-1 millisecond, color conversion at 0.3-0.5 milliseconds, normalization at 0.2-0.3 milliseconds, and the GPU copy at 0.5-1 milliseconds, totaling 3.5-7 milliseconds.\n\nThe fused CUDA kernel replaces all five steps with a single 200-500 microsecond GPU operation. The 10-20x speedup comes from three sources: elimination of CPU processing overhead, elimination of CPU-to-GPU memory transfer (the input can be acquired directly into GPU memory via V4L2 with DMABUF), and elimination of intermediate buffer reads and writes through kernel fusion.\n\nProfile the kernel with Nsight Compute to verify it is memory-bound (expected for image processing). The key metrics are achieved memory bandwidth (should be 60-80 percent of theoretical peak), arithmetic intensity (FLOPs per byte transferred), and SM occupancy. If achieved bandwidth is low, the memory access pattern needs optimization. If occupancy is low, block size or register usage needs adjustment.",
    key_takeaways: [
      "Pre-allocate all buffers and configure texture objects at initialization; reuse across all frames",
      "CUDA events provide sub-microsecond GPU timing; 1000 iterations after warm-up gives reliable statistics",
      "The fused kernel achieves 10-20x speedup over CPU OpenCV by eliminating CPU processing and host-device transfer",
      "Nsight Compute verifies the kernel is memory-bound with 60-80 percent of theoretical peak bandwidth"
    ]
  },
  {
    page: 6,
    title: "Common Pitfalls in Bare-Metal Image Preprocessing Kernels",
    content: "The most dangerous pitfall is incorrect normalization constants. If the kernel normalizes with ImageNet means (0.485, 0.456, 0.406) but the model was trained with custom means, every inference will operate on incorrectly scaled input. The model may still produce plausible-looking detections, but accuracy will be degraded in subtle ways that only appear as missed detections of small or distant objects. Always verify normalization constants against the exact training preprocessing code.\n\nInput-output layout mismatch causes similar silent failures. If the kernel writes in NHWC format but TensorRT expects NCHW, the inference engine reads interleaved channel data as if it were planar, producing garbage output. The detections will be completely wrong but the pipeline will not crash. This bug is particularly insidious because it requires careful manual verification of the output layout against the TensorRT engine's binding information.\n\nTexture coordinate precision errors in the distortion model produce slight image misalignment. If the kernel uses float32 for coordinate computation but the calibration was performed in float64, rounding errors accumulate across the image, especially at the periphery where distortion is strongest. For a 4K input image, the accumulated error can shift pixel positions by 1-2 pixels at the edges, potentially affecting detection accuracy for objects near image boundaries.\n\nOff-by-one errors in thread-to-pixel mapping cause either missing pixels (a black line at the image edge) or out-of-bounds memory access (reading past the input buffer). The most common cause is inconsistent convention between zero-indexed and one-indexed coordinates, or between inclusive and exclusive range boundaries. The kernel should map thread (0,0) to output pixel (0,0) and thread (W-1, H-1) to output pixel (W-1, H-1), with grid size ceil(W/block.x) x ceil(H/block.y) to handle non-divisible dimensions.\n\nForgetting to handle the case where grid dimensions are not perfectly divisible by block dimensions leads to out-of-bounds writes. The last blocks in each dimension have threads whose computed pixel coordinates exceed the output dimensions. These threads must return early without writing: if (out_x >= width || out_y >= height) return. Omitting this guard causes scattered memory corruption that may crash the process or silently overwrite other data structures.\n\nPerformance regression from accidentally using double-precision arithmetic. CUDA's float-to-double promotion occurs when a literal constant is written as 0.5 instead of 0.5f. The compiler promotes the surrounding computation to double precision, which runs at 1/32 the speed of float on the Jetson's GPU (which has minimal double-precision hardware). All arithmetic constants in the kernel must use the f suffix: 0.5f, 2.0f, 3.14159f.\n\nInsufficient testing of edge cases allows bugs to ship. The kernel must be tested with minimum-size input (1x1 pixel), maximum expected size, non-square dimensions, images with all-zero content (black frame from disconnected camera), and images with all-255 content (saturated sensor). Each case should produce numerically correct and bounded output that does not corrupt adjacent memory.\n\nDeployment environment differences between the development workstation and the Jetson include different CUDA driver versions, different GPU clock speeds, and different thermal constraints. A kernel that runs correctly on the development machine may exceed register limits on the Jetson or produce different floating-point results due to FMA (fused multiply-add) instruction differences. Always perform final validation on the target hardware.",
    key_takeaways: [
      "Incorrect normalization constants cause subtle accuracy degradation that only appears as missed detections",
      "NHWC vs NCHW layout mismatch produces garbage inference output without any crash or error message",
      "Use 0.5f float literals everywhere; implicit double promotion runs at 1/32 speed on Jetson GPU",
      "Always validate the final kernel on target Jetson hardware, not just the development workstation"
    ]
  },
  {
    page: 7,
    title: "Advanced Topics: CUDA Graphs, Stream Priority, and Deterministic Scheduling",
    content: "CUDA Graphs capture a sequence of GPU operations (kernel launches, memory copies, events) into a reusable execution template. Once captured, the graph can be launched with a single API call that replays the entire sequence with minimal host-side overhead. For a drone's perception pipeline consisting of a fused preprocessing kernel followed by TensorRT inference followed by a custom NMS kernel, the CUDA graph captures all three stages and replays them with three fewer kernel launch expenses per frame.\n\nGraph capture requires that the sequence of operations is identical for every replay. This means the input and output buffer sizes must be fixed, the kernel grid dimensions must be constant, and no host-side branching can affect the GPU operation sequence. For a preprocessing kernel with fixed input and output dimensions, these requirements are naturally satisfied. For NMS where the number of detections varies, the grid must be configured for the maximum possible detection count, with masking for unused threads.\n\nThe performance benefit of CUDA graphs comes primarily from eliminating host-side CPU work for each kernel launch. Without graphs, each kernel launch involves driver calls, parameter validation, and scheduling overhead totaling 5-15 microseconds per launch. For three launches per frame at 30 FPS, this is 450-1350 microseconds per second spent on launch overhead. CUDA graphs reduce this to a single launch operation per frame.\n\nStream priority enables the GPU scheduler to preempt lower-priority work in favor of higher-priority work. Creating a high-priority stream for the critical perception kernels and a low-priority stream for non-critical work (debug visualization, telemetry encoding) ensures that the perception pipeline is never delayed by elective processing. Priority levels are integer values where lower numbers indicate higher priority, queried with cudaDeviceGetStreamPriorityRange.\n\nDeterministic scheduling on the Jetson requires configuring the system beyond CUDA. The jetson_clocks utility locks CPU and GPU frequencies to their maximum values, eliminating DVFS-induced latency variation. The nvpmodel utility selects a power mode that determines the maximum frequencies and the number of active CPU cores. For maximum determinism during flight operations, jetson_clocks should be run after boot and nvpmodel should be set to MAXN (maximum performance) mode.\n\nThread affinity pins the ROS 2 executor and CUDA launch thread to specific CPU cores, preventing the OS scheduler from migrating them between cores (which invalidates caches and adds migration latency). The Jetson Orin Nano has 6 ARM Cortex-A78AE cores. Dedicating cores 4 and 5 to the perception pipeline (using pthread_setaffinity_np or taskset) and leaving cores 0-3 for ROS 2 communication, system services, and other nodes provides isolation between timing-critical and best-effort workloads.\n\nIRQ affinity directs hardware interrupt handling to specific cores. Camera frame-ready interrupts should be directed to the same core running the perception thread to minimize wake-up latency. Network interrupts (for MAVROS communication) should be directed to the communication core. The /proc/irq/[IRQ]/smp_affinity file controls interrupt routing per interrupt line.\n\nThe combined effect of CUDA graphs, stream priority, frequency locking, thread affinity, and IRQ routing is a system with bounded worst-case latency. Each optimization addresses a specific source of timing variability, and together they transform the Jetson from a general-purpose computer with variable scheduling into a near-real-time perception platform with predictable execution timing.",
    key_takeaways: [
      "CUDA Graphs capture the complete pipeline (preprocess, inference, NMS) for single-call replay with minimal overhead",
      "Stream priority ensures perception kernels preempt non-critical GPU work like visualization",
      "jetson_clocks and nvpmodel lock frequencies to eliminate DVFS-induced latency variation",
      "Thread affinity plus IRQ routing isolate timing-critical perception from best-effort OS workloads"
    ]
  },
  {
    page: 8,
    title: "Quiz Deep Dive: Custom CUDA Kernels for Deterministic Edge Execution",
    content: "The quiz question asks why we write custom CUDA kernels for edge execution. The correct answer is to bypass software abstraction overhead and guarantee deterministic microsecond latencies. This answer addresses two distinct requirements that standard frameworks fail to satisfy simultaneously: low absolute latency and bounded worst-case execution time.\n\nBypassing software abstraction overhead means executing the computation directly on the GPU without the intermediation of Python interpreters, framework dispatch systems, memory pool managers, or autograd engines. Each of these layers exists to provide developer convenience at the cost of runtime overhead. A Python function call takes approximately 80 nanoseconds. A PyTorch tensor operation involves dispatcher lookup, autograd graph recording, memory allocation checking, and CUDA API wrapping, totaling 10-50 microseconds before any GPU work begins. For operations called thousands of times per frame (pixel-level preprocessing), this overhead accumulates to milliseconds.\n\nGuaranteeing deterministic microsecond latencies requires eliminating not just the average overhead but all sources of latency variability. Framework implementations introduce variability through: Python's garbage collector (which can pause all threads for 1-10 milliseconds), CUDA's lazy context initialization (which adds milliseconds to the first kernel launch), dynamic memory allocation (which varies in latency depending on the memory pool's fragmentation state), and implicit synchronization (where one kernel waits for another to complete due to shared memory resources).\n\nA custom CUDA kernel with pre-allocated buffers, a pre-initialized CUDA context, explicit synchronization, and no Python in the execution path eliminates all four variability sources. The kernel's execution time depends only on the GPU hardware's processing speed, which is deterministic when clock frequencies are locked. The measured jitter (standard deviation of execution time) for a well-optimized custom kernel is typically under 10 microseconds, compared to 1-5 milliseconds for the equivalent framework operation.\n\nThe microsecond latency target is specific to the application domain. A 100 Hz flight controller iterates every 10 milliseconds. If perception uses 7 milliseconds and communication uses 2 milliseconds, only 1 millisecond remains for all post-processing operations (NMS, coordinate transformation, decision logic). Framework post-processing consuming 3-5 milliseconds violates this budget. Custom CUDA post-processing consuming 100-300 microseconds fits comfortably.\n\nThe question's emphasis on edge execution highlights that these constraints are specific to embedded deployment. In the cloud, a 5-millisecond overhead per frame is negligible. On a drone's edge computer with a strict real-time budget and no opportunity to horizontally scale, every microsecond is precious. The edge constraint makes bare-metal optimization not just beneficial but necessary for safety-critical autonomous operation.\n\nThis principle applies beyond NMS and preprocessing. Any pipeline stage in the timing-critical path that introduces framework overhead is a candidate for bare-metal replacement: coordinate transformation from camera to world frame, sensor fusion combining lidar and camera detections, obstacle map generation from point clouds, and trajectory planning computations. The decision to replace depends on profiling data showing that the specific stage's framework overhead exceeds the available timing margin.",
    key_takeaways: [
      "Framework overhead comes from Python dispatch, autograd tracking, memory pool management, and implicit synchronization",
      "Custom CUDA kernels achieve sub-10-microsecond jitter versus 1-5 millisecond jitter from framework implementations",
      "The 100 Hz flight controller budget (10ms per iteration) leaves minimal margin for post-processing overhead",
      "Any timing-critical pipeline stage exceeding its budget in profiling data is a candidate for bare-metal replacement"
    ]
  },
  {
    page: 9,
    title: "Integration Patterns: End-to-End Pipeline Assembly on the Jetson",
    content: "The complete perception pipeline on the Jetson integrates camera acquisition, fused preprocessing, TensorRT inference, custom NMS, and ROS 2 output publishing. Each stage uses bare-metal or optimized implementations where profiling identified timing violations, while retaining framework solutions where timing constraints are comfortable.\n\nCamera frame acquisition on the Jetson uses GStreamer with the nvarguscamerasrc plugin, which reads frames from the CSI camera into GPU-accessible DMABUF memory. This zero-copy path means the camera frame arrives in GPU memory without any CPU-GPU transfer. The fused preprocessing kernel reads directly from this DMABUF-backed buffer, eliminating the largest single latency contributor in a CPU-based preprocessing pipeline.\n\nThe pipeline uses triple buffering to decouple frame acquisition from processing. While the GPU processes frame N, the camera DMAs frame N+1 into a second buffer, and frame N-1's detection results are being published to ROS 2. Three buffers ensure that no stage waits for another, maximizing pipeline utilization. The buffer rotation is managed by the host thread using three pre-allocated BufferSet structures, each containing an input buffer, preprocessing output buffer, inference output buffer, and NMS output buffer.\n\nCUDA stream assignment partitions the pipeline for maximum overlap. The preprocessing kernel runs on stream A. TensorRT inference runs on stream B. NMS runs on stream C. CUDA events synchronize between stages: the inference waits for a preprocessing-complete event, and NMS waits for an inference-complete event. Between frames, preprocessing for frame N+1 starts on stream A immediately after frame N's preprocessing completes, overlapping with frame N's inference on stream B.\n\nROS 2 integration publishes detection results as vision_msgs/Detection2DArray messages from a C++ node. The publisher is configured with a QoS profile that uses BEST_EFFORT reliability (dropping messages rather than buffering if the subscriber is slow) and keeps only the latest message (depth 1). This prevents message queue buildup that could cause memory growth and latency spikes over long missions.\n\nDiagnostic instrumentation records per-frame timing data without impacting the critical path. CUDA events measure GPU stage durations asynchronously. The host thread reads event timestamps after each frame and publishes a diagnostic_msgs/DiagnosticArray message with the timing breakdown. A separate monitoring node subscribes to diagnostics and raises an alert if any stage exceeds its latency budget for multiple consecutive frames.\n\nGraceful degradation handles transient timing violations. If a single frame's total loop latency exceeds the budget (detected by comparing CUDA event timestamps), the pipeline drops the next frame's preprocessing and reuses the previous frame's detection results. This reduces the effective frame rate from 30 to 15 FPS for one cycle but prevents cascading latency that could violate the flight controller's timeout. The flight controller receives stale but recent detection data rather than no data.\n\nEndurance testing validates that the pipeline runs for the full mission duration (typically 20-40 minutes for battery-powered drones) without memory growth, latency degradation, or thermal throttling beyond the profiled steady-state levels. The test logs every frame's timing data and plots the distribution over time. A healthy system shows a tight timing distribution that remains constant throughout the mission. A degrading system shows increasing latency variance over time, indicating a memory leak or thermal issue.\n\nThe final deployment artifact is a ROS 2 launch file that starts the perception node with all parameters: camera configuration, TensorRT plan file path, NMS threshold, timing budgets, and diagnostic publication rate. The launch file also configures CPU affinity, stream priority, and GPU clock mode. This single file captures the complete deployment configuration, enabling reproducible deployment across the drone fleet.",
    key_takeaways: [
      "Zero-copy camera acquisition via DMABUF eliminates CPU-GPU transfer; triple buffering decouples pipeline stages",
      "CUDA stream assignment with event synchronization enables preprocessing-inference-NMS overlap across frames",
      "Graceful degradation drops frames rather than cascading latency violations to the flight controller",
      "Endurance testing validates stable timing distribution over the full mission duration"
    ]
  },
  {
    page: 10,
    title: "Summary and Self-Assessment",
    content: "This lesson explored bare-metal CUDA kernel optimization through the lens of image preprocessing for drone perception, complementing the previous lesson's coverage of custom NMS kernels. Both applications share the same fundamental motivation: framework abstractions introduce latency and jitter that violate the timing constraints of safety-critical autonomous systems.\n\nThe fused preprocessing kernel combines undistortion, resizing, color conversion, and normalization into a single GPU pass, reducing processing time from 3-8 milliseconds (CPU OpenCV) to 200-500 microseconds. The 10-20x speedup comes from eliminating CPU processing, host-device memory transfers, intermediate buffer traffic, and kernel launch overhead. The kernel uses texture memory for hardware-accelerated interpolation and constant memory for camera calibration parameters.\n\nGPU performance optimization addresses four primary concerns: warp divergence (minimized by eliminating branches and organizing threads for spatial coherence), shared memory bank conflicts (resolved through padding or planar data layouts), memory coalescing (ensured by aligning thread access patterns with memory structure), and register pressure (monitored via compiler flags and managed through shared memory offloading).\n\nDeterministic execution requires system-level configuration beyond CUDA programming: frequency locking with jetson_clocks, thread affinity for CPU core isolation, IRQ routing for interrupt handling, stream priority for GPU scheduling, and CUDA graphs for minimal launch overhead. Together these eliminate the variability sources that make framework implementations unsuitable for real-time flight control.\n\nPipeline integration uses zero-copy camera acquisition, triple buffering, multi-stream execution with event synchronization, and graceful degradation for transient timing violations. The assembled pipeline runs as a C++ ROS 2 node with pre-allocated resources, publishing detection results and diagnostic timing data.\n\nSelf-assessment questions: First, explain why a fused preprocessing kernel is faster than four separate kernels performing the same operations, identifying specifically which overheads are eliminated. Second, describe how texture memory improves the performance of an undistortion kernel compared to raw global memory reads. Third, explain the relationship between shared memory bank conflicts and the choice between interleaved and planar pixel storage. Fourth, design a CUDA graph that captures the complete perception pipeline and explain what constraints this places on the input format. Fifth, given a drone with a 10-millisecond perception budget where profile data shows 2 milliseconds for preprocessing, 6 milliseconds for inference, and 1 millisecond for NMS, justify which stage to optimize with bare-metal CUDA and explain your reasoning.",
    key_takeaways: [
      "Fused CUDA preprocessing achieves 10-20x speedup over CPU OpenCV by eliminating transfers and intermediate buffers",
      "System-level configuration (frequency locking, thread affinity, IRQ routing) complements CUDA optimization for determinism",
      "The complete pipeline combines zero-copy acquisition, triple buffering, multi-stream execution, and graceful degradation"
    ]
  }
];

data.tracks[3].lessons[5].learning_script = learningScript;

fs.writeFileSync(filePath, JSON.stringify(data, null, 2), "utf-8");
console.log("Injected learning script for Edge AI Lesson 6: Bare-Metal Kernel Optimization (Variant)");
console.log("Pages: " + learningScript.length);
